
### Shell environment ready to run bitake commands. ###

You can now run 'bitbake <target>'

Common targets that build full machine image are:
    machine-image

Run 'bitbake <pkg-name>' to build a required package, example:
    bitbake lk

